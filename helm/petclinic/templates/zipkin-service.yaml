apiVersion: v1
kind: Service
metadata:
  name: zipkin
  namespace: {{ .Release.Namespace }}
spec:
  selector:
    app: zipkin
  ports:
  - protocol: TCP
    port: {{ .Values.zipkin.port }}
    targetPort: {{ .Values.zipkin.port }}
    nodePort: 30100
  type: NodePort