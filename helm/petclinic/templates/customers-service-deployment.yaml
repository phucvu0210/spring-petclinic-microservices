apiVersion: apps/v1
kind: Deployment
metadata:
  name: customers-service
  namespace: {{ .Release.Namespace }}
spec:
  replicas: 1
  selector:
    matchLabels:
      app: customers-service
  template:
    metadata:
      labels:
        app: customers-service
    spec:
      containers:
      - name: customers-service
        image: {{ .Values.customers-service.repository }}:{{ .Values.customers-service.tag }}
        ports:
        - containerPort: {{ .Values.customers-service.port }}
        env:
        - name: SPRING_PROFILES_ACTIVE
          value: docker