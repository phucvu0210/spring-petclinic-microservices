apiVersion: apps/v1
kind: Deployment
metadata:
  name: admin-server
  namespace: {{ .Release.Namespace }}
spec:
  replicas: 1
  selector:
    matchLabels:
      app: admin-server
  template:
    metadata:
      labels:
        app: admin-server
    spec:
      containers:
      - name: admin-server
        image: {{ .Values.admin-server.repository }}:{{ .Values.admin-server.tag }}
        ports:
        - containerPort: {{ .Values.admin-server.port }}
        env:
        - name: SPRING_PROFILES_ACTIVE
          value: docker