apiVersion: v1
kind: Service
metadata:
  name: config-server
  namespace: {{ .Release.Namespace }}
spec:
  selector:
    app: config-server
  ports:
  - protocol: TCP
    port: {{ .Values.config-server.port }}
    targetPort: {{ .Values.config-server.port }}
    nodePort: 30120
  type: NodePort