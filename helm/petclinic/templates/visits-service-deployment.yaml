apiVersion: apps/v1
kind: Deployment
metadata:
  name: visits-service
  namespace: {{ .Release.Namespace }}
spec:
  replicas: 1
  selector:
    matchLabels:
      app: visits-service
  template:
    metadata:
      labels:
        app: visits-service
    spec:
      containers:
      - name: visits-service
        image: {{ .Values.visits-service.repository }}:{{ .Values.visits-service.tag }}
        ports:
        - containerPort: {{ .Values.visits-service.port }}
        env:
        - name: SPRING_PROFILES_ACTIVE
          value: docker